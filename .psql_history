SELECT *FROM Instalaciones, Pertenece, PuertosWHERE instalaciones.iid = pertenece.iid and puertos.pid = pertenece.pidINTERSECT SELECT *FROM AstillerosWHERE instalaciones.iid = astilleros.iid;
(SELECT instalaciones.iid, puertos.pidFROM Instalaciones, Pertenece, PuertosWHERE instalaciones.iid = pertenece.iid and puertos.pid = pertenece.pid)INTERSECT SELECT *FROM AstillerosWHERE instalaciones.iid = astilleros.iid;
SELECT *FROM Astilleros, Pertenece, PuertosWHERE Astilleros.iid = pertenece.iid and puertos.pid = pertenece.pid;
SELECT Puertos.pid, Puertos.nombreFROM Astilleros, Pertenece, PuertosWHERE Astilleros.iid = pertenece.iid and puertos.pid = pertenece.pid;
Select * From puertos;
SELECT *FROM jefe, personal,(SELECT instalaciones.iid, puertos.nombreFROM puertos, pertenece, instalacionesWHERE puertos.pid = pertenece.pid and instalaciones.iid = pertenece.iid and puertos.nombre = 'Mejillones') AS ppiWHERE jefe.rut = personal.rut and jefe.iid = ppi.iid;
q
\q
SELECT *FROM jefe, personal,(SELECT instalaciones.iid, puertos.nombreFROM puertos, pertenece, instalacionesWHERE puertos.pid = pertenece.pid and instalaciones.iid = pertenece.iid and puertos.nombre = 'Mejillones') AS ppiWHERE jefe.rut = personal.rut and jefe.iid = ppi.iid;
SELECT* FROM puertos;
pull
\q
\c grupo75
SELECT * FROM barcos, sobre, permisos, instalaciones (SELECT * FROM para_a UNION SELECT ALL FROM para_m) AS upam WHERE barcos.patente = sobre.patente and sobre.per_id = permisos.per_id and upam.iid = instalaciones.iid;
SELECT * FROM barcos, sobre, permisos, instalaciones (SELECT * FROM para_a UNION SELECT FROM para_m) AS upam WHERE barcos.patente = sobre.patente and sobre.per_id = permisos.per_id and upam.iid = instalaciones.iid;
SELECT * FROM barcos, sobre, permisos, instalaciones (SELECT * FROM para_a UNION SELECT * FROM para_m) AS upam WHERE barcos.patente = sobre.patente and sobre.per_id = permisos.per_id and upam.iid = instalaciones.iid;
SELECT * FROM barcos, sobre, permisos, instalaciones (SELECT * FROM para_a UNION SELECT ALL * FROM para_m) AS upam WHERE barcos.patente = sobre.patente and sobre.per_id = permisos.per_id and upam.iid = instalaciones.iid;
SELECT * FROM para_a UNION SELECT ALL * FROM para_m;
\DT
\dt
q
\q
\c grupo75e2
git pullpullq
\q
\c grupo75e2
cd
\q
\c grupo75e2
ls
\q
\q
git pull
\q
\c grupo75e2
git pullpull
\q
\c grupo75e2
git pull
\q
\c grupo75e2
git pullpulllsSELECT * FROM PUERTOS;
SELECT * FROM PUERTOS;
\q
git pullas;
\q
\c grupo75e2
cd Sitesqexit
\q
\c grupo75e2
git pullexit
\q
\c grupo75e2
SELECT personal.rut, personal.nombre, personal.edad, personal.sexo FROM jefe, personal,  (SELECT instalaciones.iid, puertos.nombre  FROM puertos, pertenece, instalaciones  WHERE puertos.pid = pertenece.pid   and instalaciones.iid = pertenece.iid and puertos.nombre = 'Arica') AS ppi  WHERE jefe.rut = personal.rut and jefe.iid = ppi.iid;
Select * from puertos;
SELECT personal.rut, personal.nombre, personal.edad, personal.sexo FROM jefe, personal,  (SELECT instalaciones.iid, puertos.nombre  FROM puertos, pertenece, instalaciones  WHERE puertos.pid = pertenece.pid   and instalaciones.iid = pertenece.iid and puertos.nombre = 'Mejillones') AS ppi  WHERE jefe.rut = personal.rut and jefe.iid = ppi.iid;
exit
\q
\c grupo75e2
SELECT * FROM esta;
SELECT parte1.iid, parte1.patente, parte1.nombre, ubicacion.ciudad, puertos.nombre, parte1.atraque FROM pertenece, puertos, esta, ubicacion, (SELECT upam.iid, barcos.patente, barcos.nombre, barcos.pais, permisos.per_id, permisos.atraqueFROM barcos, sobre, permisos, instalaciones,(SELECT * FROM para_a UNION SELECT ALL * FROM para_m) AS upam WHERE barcos.patente = sobre.patenteand barcos.nombre = 'Calypso' and sobre.per_id = permisos.per_idand upam.iid = instalaciones.iid and permisos.per_id = upam.per_id) AS parte1 WHERE parte1.iid = pertenece.iid and pertenece.pid = puertos.pid and puertos.pid = esta.pidand esta.ciudad = ubicacion.ciudad and ubicacion.ciudad = 'Arica';
SELECT MAX(parte3.count) FROM (SELECT parte2.nombre, COUNT (*) FROM (SELECT parte1.iid, parte1.patente, puertos.nombre, parte1.atraque FROM pertenece, puertos, (SELECT upam.iid, barcos.patente, barcos.pais, permisos.per_id, permisos.atraqueFROM barcos, sobre, permisos, instalaciones,(SELECT * FROM para_a UNION SELECT ALL * FROM para_m) AS upam WHERE barcos.patente = sobre.patente and sobre.per_id = permisos.per_idand upam.iid = instalaciones.iid and permisos.per_id = upam.per_id) AS parte1 WHERE parte1.iid = pertenece.iid and pertenece.pid = puertos.pid) AS parte2 GROUP BY parte2.nombre) AS parte3;
SELECT parte2.nombre, COUNT(parte2.nombre) as totalFROM (SELECT parte1.iid, parte1.patente, puertos.nombre, parte1.atraque FROM pertenece, puertos, (SELECT upam.iid, barcos.patente, barcos.pais, permisos.per_id, permisos.atraqueFROM barcos, sobre, permisos, instalaciones,(SELECT * FROM para_a UNION SELECT ALL * FROM para_m) AS upam WHERE barcos.patente = sobre.patente and sobre.per_id = permisos.per_idand upam.iid = instalaciones.iid and permisos.per_id = upam.per_id) AS parte1 WHERE parte1.iid = pertenece.iid and pertenece.pid = puertos.pid) AS parte2GROUP BY parte2.nombreORDER BY total DESC;
SELECT name FROM (SELECT parte2.nombre, COUNT(parte2.nombre) as totalFROM (SELECT parte1.iid, parte1.patente, puertos.nombre, parte1.atraque FROM pertenece, puertos, (SELECT upam.iid, barcos.patente, barcos.pais, permisos.per_id, permisos.atraqueFROM barcos, sobre, permisos, instalaciones,(SELECT * FROM para_a UNION SELECT ALL * FROM para_m) AS upam WHERE barcos.patente = sobre.patente and sobre.per_id = permisos.per_idand upam.iid = instalaciones.iid and permisos.per_id = upam.per_id) AS parte1 WHERE parte1.iid = pertenece.iid and pertenece.pid = puertos.pid) AS parte2GROUP BY parte2.nombreORDER BY total DESC) AS parte3 WHERE total = (SELECT MAX(total) FROM parte3);
SELECT parte3.nombre FROM (SELECT parte2.nombre, COUNT(parte2.nombre) as totalFROM (SELECT parte1.iid, parte1.patente, puertos.nombre, parte1.atraque FROM pertenece, puertos, (SELECT upam.iid, barcos.patente, barcos.pais, permisos.per_id, permisos.atraqueFROM barcos, sobre, permisos, instalaciones,(SELECT * FROM para_a UNION SELECT ALL * FROM para_m) AS upam WHERE barcos.patente = sobre.patente and sobre.per_id = permisos.per_idand upam.iid = instalaciones.iid and permisos.per_id = upam.per_id) AS parte1 WHERE parte1.iid = pertenece.iid and pertenece.pid = puertos.pid) AS parte2GROUP BY parte2.nombreORDER BY total DESC) AS parte3 WHERE total = (SELECT MAX(total) FROM parte3);
SELECT * FROM (SELECT parte2.nombre, COUNT(parte2.nombre) as totalFROM (SELECT parte1.iid, parte1.patente, puertos.nombre, parte1.atraque FROM pertenece, puertos, (SELECT upam.iid, barcos.patente, barcos.pais, permisos.per_id, permisos.atraqueFROM barcos, sobre, permisos, instalaciones,(SELECT * FROM para_a UNION SELECT ALL * FROM para_m) AS upam WHERE barcos.patente = sobre.patente and sobre.per_id = permisos.per_idand upam.iid = instalaciones.iid and permisos.per_id = upam.per_id) AS parte1 WHERE parte1.iid = pertenece.iid and pertenece.pid = puertos.pid) AS parte2GROUP BY parte2.nombreORDER BY total DESC) AS parte3 WHERE total = (SELECT MAX(total) FROM parte3);
SELECT * FROM (SELECT parte2.nombre, COUNT(parte2.nombre) as totalFROM (SELECT parte1.iid, parte1.patente, puertos.nombre, parte1.atraque FROM pertenece, puertos, (SELECT upam.iid, barcos.patente, barcos.pais, permisos.per_id, permisos.atraqueFROM barcos, sobre, permisos, instalaciones,(SELECT * FROM para_a UNION SELECT ALL * FROM para_m) AS upam WHERE barcos.patente = sobre.patente and sobre.per_id = permisos.per_idand upam.iid = instalaciones.iid and permisos.per_id = upam.per_id) AS parte1 WHERE parte1.iid = pertenece.iid and pertenece.pid = puertos.pid) AS parte2GROUP BY parte2.nombreORDER BY total DESC) WHERE total = (SELECT MAX(total) FROM (SELECT upam.iid, barcos.patente, barcos.pais, permisos.per_id, permisos.atraqueFROM barcos, sobre, permisos, instalaciones,(SELECT * FROM para_a UNION SELECT ALL * FROM para_m) AS upam WHERE barcos.patente = sobre.patente and sobre.per_id = permisos.per_idand upam.iid = instalaciones.iid and permisos.per_id = upam.per_id) AS parte1 WHERE parte1.iid = pertenece.iid and pertenece.pid = puertos.pid) AS parte2GROUP BY parte2.nombreORDER BY total DESC));
SELECT * FROM (SELECT parte2.nombre, COUNT(parte2.nombre) as totalFROM (SELECT parte1.iid, parte1.patente, puertos.nombre, parte1.atraque FROM pertenece, puertos, (SELECT upam.iid, barcos.patente, barcos.pais, permisos.per_id, permisos.atraqueFROM barcos, sobre, permisos, instalaciones,(SELECT * FROM para_a UNION SELECT ALL * FROM para_m) AS upam WHERE barcos.patente = sobre.patente and sobre.per_id = permisos.per_idand upam.iid = instalaciones.iid and permisos.per_id = upam.per_id) AS parte1 WHERE parte1.iid = pertenece.iid and pertenece.pid = puertos.pid) AS parte2GROUP BY parte2.nombreORDER BY total DESC) AS foo WHERE total = (SELECT MAX(total) FROM (SELECT upam.iid, barcos.patente, barcos.pais, permisos.per_id, permisos.atraqueFROM barcos, sobre, permisos, instalaciones,(SELECT * FROM para_a UNION SELECT ALL * FROM para_m) AS upam WHERE barcos.patente = sobre.patente and sobre.per_id = permisos.per_idand upam.iid = instalaciones.iid and permisos.per_id = upam.per_id) AS parte1 WHERE parte1.iid = pertenece.iid and pertenece.pid = puertos.pid) AS parte2GROUP BY parte2.nombreORDER BY total DESC));
SELECT (parte2.nombre, COUNT(parte2.nombre) as totalFROM (SELECT parte1.iid, parte1.patente, puertos.nombre, parte1.atraque FROM pertenece, puertos, (SELECT upam.iid, barcos.patente, barcos.pais, permisos.per_id, permisos.atraqueFROM barcos, sobre, permisos, instalaciones,(SELECT * FROM para_a UNION SELECT ALL * FROM para_m) AS upam WHERE barcos.patente = sobre.patente and sobre.per_id = permisos.per_idand upam.iid = instalaciones.iid and permisos.per_id = upam.per_id) AS parte1 WHERE parte1.iid = pertenece.iid and pertenece.pid = puertos.pid) AS parte2GROUP BY parte2.nombreORDER BY total DESC) AS parte3 ;
SELECT parte2.nombre, COUNT(parte2.nombre) as totalFROM (SELECT parte1.iid, parte1.patente, puertos.nombre, parte1.atraque FROM pertenece, puertos, (SELECT upam.iid, barcos.patente, barcos.pais, permisos.per_id, permisos.atraqueFROM barcos, sobre, permisos, instalaciones,(SELECT * FROM para_a UNION SELECT ALL * FROM para_m) AS upam WHERE barcos.patente = sobre.patente and sobre.per_id = permisos.per_idand upam.iid = instalaciones.iid and permisos.per_id = upam.per_id) AS parte1 WHERE parte1.iid = pertenece.iid and pertenece.pid = puertos.pid) AS parte2GROUP BY parte2.nombreORDER BY total DESC;
SELECT parte2.nombre, parte2.atraque COUNT(parte2.nombre) as totalFROM (SELECT parte1.iid, parte1.patente, puertos.nombre, parte1.atraque FROM pertenece, puertos, (SELECT upam.iid, barcos.patente, barcos.pais, permisos.per_id, permisos.atraqueFROM barcos, sobre, permisos, instalaciones,(SELECT * FROM para_a UNION SELECT ALL * FROM para_m) AS upam WHERE barcos.patente = sobre.patente and sobre.per_id = permisos.per_idand upam.iid = instalaciones.iid and permisos.per_id = upam.per_id) AS parte1 WHERE parte1.iid = pertenece.iid and pertenece.pid = puertos.pid) AS parte2GROUP BY parte2.nombreORDER BY total DESC;
SELECT parte2.nombre, parte2.atraque, COUNT(parte2.nombre) as totalFROM (SELECT parte1.iid, parte1.patente, puertos.nombre, parte1.atraque FROM pertenece, puertos, (SELECT upam.iid, barcos.patente, barcos.pais, permisos.per_id, permisos.atraqueFROM barcos, sobre, permisos, instalaciones,(SELECT * FROM para_a UNION SELECT ALL * FROM para_m) AS upam WHERE barcos.patente = sobre.patente and sobre.per_id = permisos.per_idand upam.iid = instalaciones.iid and permisos.per_id = upam.per_id) AS parte1 WHERE parte1.iid = pertenece.iid and pertenece.pid = puertos.pid) AS parte2GROUP BY parte2.nombreORDER BY total DESC;
SELECT parte2.nombre, parte2.atraque, COUNT(parte2.nombre) as totalFROM (SELECT parte1.iid, parte1.patente, puertos.nombre, parte1.atraque FROM pertenece, puertos, (SELECT upam.iid, barcos.patente, barcos.pais, permisos.per_id, permisos.atraqueFROM barcos, sobre, permisos, instalaciones,(SELECT * FROM para_a UNION SELECT ALL * FROM para_m) AS upam WHERE barcos.patente = sobre.patente and sobre.per_id = permisos.per_idand upam.iid = instalaciones.iid and permisos.per_id = upam.per_id) AS parte1 WHERE parte1.iid = pertenece.iid and pertenece.pid = puertos.pid) AS parte2GROUP BY parte2.nombre, parte2.atraque,ORDER BY total DESC;
SELECT parte2.nombre, parte2.atraque, COUNT(parte2.nombre) as totalFROM (SELECT parte1.iid, parte1.patente, puertos.nombre, parte1.atraque FROM pertenece, puertos, (SELECT upam.iid, barcos.patente, barcos.pais, permisos.per_id, permisos.atraqueFROM barcos, sobre, permisos, instalaciones,(SELECT * FROM para_a UNION SELECT ALL * FROM para_m) AS upam WHERE barcos.patente = sobre.patente and sobre.per_id = permisos.per_idand upam.iid = instalaciones.iid and permisos.per_id = upam.per_id) AS parte1 WHERE parte1.iid = pertenece.iid and pertenece.pid = puertos.pid) AS parte2GROUP BY parte2.nombre, parte2.atraqueORDER BY total DESC;
\c grupo75e2
\grupo75e2
\c grupo75e2
SELECT parte2.nombre, parte2.atraque, COUNT(parte2.nombre) as totalFROM (SELECT parte1.iid, parte1.patente, puertos.nombre, parte1.atraque FROM pertenece, puertos, (SELECT upam.iid, barcos.patente, barcos.pais, permisos.per_id, permisos.atraqueFROM barcos, sobre, permisos, instalaciones,(SELECT * FROM para_a UNION SELECT ALL * FROM para_m) AS upam WHERE barcos.patente = sobre.patente and sobre.per_id = permisos.per_idand upam.iid = instalaciones.iid and permisos.per_id = upam.per_id and permisos.atraque LIKE '%2020-08%') AS parte1 WHERE parte1.iid = pertenece.iid and pertenece.pid = puertos.pid) AS parte2GROUP BY parte2.nombre, parte2.atraqueORDER BY total DESC;
SELECT parte2.nombre, parte2.atraque, COUNT(parte2.nombre) as totalFROM (SELECT parte1.iid, parte1.patente, puertos.nombre, parte1.atraque FROM pertenece, puertos, (SELECT upam.iid, barcos.patente, barcos.pais, permisos.per_id, permisos.atraqueFROM barcos, sobre, permisos, instalaciones,(SELECT * FROM para_a UNION SELECT ALL * FROM para_m) AS upam WHERE barcos.patente = sobre.patente and sobre.per_id = permisos.per_idand upam.iid = instalaciones.iid and permisos.per_id = upam.per_id and permisos.atraque LIKE %2020-08%) AS parte1 WHERE parte1.iid = pertenece.iid and pertenece.pid = puertos.pid) AS parte2GROUP BY parte2.nombre, parte2.atraqueORDER BY total DESC;
SELECT parte2.nombre, parte2.atraque, COUNT(parte2.nombre) as totalFROM (SELECT parte1.iid, parte1.patente, puertos.nombre, parte1.atraque FROM pertenece, puertos, (SELECT upam.iid, barcos.patente, barcos.pais, permisos.per_id, permisos.atraqueFROM barcos, sobre, permisos, instalaciones,(SELECT * FROM para_a UNION SELECT ALL * FROM para_m) AS upam WHERE barcos.patente = sobre.patente and sobre.per_id = permisos.per_idand upam.iid = instalaciones.iid and permisos.per_id = upam.per_id and permisos.atraque LIKE %2020%) AS parte1 WHERE parte1.iid = pertenece.iid and pertenece.pid = puertos.pid) AS parte2GROUP BY parte2.nombre, parte2.atraqueORDER BY total DESC;
SELECT parte2.nombre, parte2.atraque, COUNT(parte2.nombre) as totalFROM (SELECT parte1.iid, parte1.patente, puertos.nombre, parte1.atraque FROM pertenece, puertos, (SELECT upam.iid, barcos.patente, barcos.pais, permisos.per_id, permisos.atraqueFROM barcos, sobre, permisos, instalaciones,(SELECT * FROM para_a UNION SELECT ALL * FROM para_m) AS upam WHERE barcos.patente = sobre.patente and sobre.per_id = permisos.per_idand upam.iid = instalaciones.iid and permisos.per_id = upam.per_id and permisos.atraque LIKE '%2020%') AS parte1 WHERE parte1.iid = pertenece.iid and pertenece.pid = puertos.pid) AS parte2GROUP BY parte2.nombre, parte2.atraqueORDER BY total DESC;
SELECT parte2.nombre, parte2.atraque, COUNT(parte2.nombre) as totalFROM (SELECT parte1.iid, parte1.patente, puertos.nombre, parte1.atraque FROM pertenece, puertos, (SELECT upam.iid, barcos.patente, barcos.pais, permisos.per_id, permisos.atraqueFROM barcos, sobre, permisos, instalaciones,(SELECT * FROM para_a UNION SELECT ALL * FROM para_m) AS upam WHERE barcos.patente = sobre.patente and sobre.per_id = permisos.per_idand upam.iid = instalaciones.iid and permisos.per_id = upam.per_id and permisos.atraque LIKE '%2020-08-13%') AS parte1 WHERE parte1.iid = pertenece.iid and pertenece.pid = puertos.pid) AS parte2GROUP BY parte2.nombre, parte2.atraqueORDER BY total DESC;
SELECT parte2.nombre, parte2.atraque, COUNT(parte2.nombre) as totalFROM (SELECT parte1.iid, parte1.patente, puertos.nombre, parte1.atraque FROM pertenece, puertos, (SELECT upam.iid, barcos.patente, barcos.pais, permisos.per_id, permisos.atraqueFROM barcos, sobre, permisos, instalaciones,(SELECT * FROM para_a UNION SELECT ALL * FROM para_m) AS upam WHERE barcos.patente = sobre.patente and sobre.per_id = permisos.per_idand upam.iid = instalaciones.iid and permisos.per_id = upam.per_id and permisos.atraque and LIKE '%2020-08%') AS parte1 WHERE parte1.iid = pertenece.iid and pertenece.pid = puertos.pid) AS parte2GROUP BY parte2.nombre, parte2.atraqueORDER BY total DESC;
SELECT parte2.nombre, parte2.atraque, COUNT(parte2.nombre) as totalFROM (SELECT parte1.iid, parte1.patente, puertos.nombre, parte1.atraque FROM pertenece, puertos, (SELECT upam.iid, barcos.patente, barcos.pais, permisos.per_id, permisos.atraqueFROM barcos, sobre, permisos, instalaciones,(SELECT * FROM para_a UNION SELECT ALL * FROM para_m) AS upam WHERE barcos.patente = sobre.patente and sobre.per_id = permisos.per_idand upam.iid = instalaciones.iid and permisos.per_id = upam.per_id and permisos.atraque LIKE '%2020-08%') AS parte1 WHERE parte1.iid = pertenece.iid and pertenece.pid = puertos.pid) AS parte2GROUP BY parte2.nombre, parte2.atraqueORDER BY total DESC;
SELECT parte2.nombre, parte2.atraque, COUNT(parte2.nombre) as totalFROM (SELECT parte1.iid, parte1.patente, puertos.nombre, parte1.atraque FROM pertenece, puertos, (SELECT upam.iid, barcos.patente, barcos.pais, permisos.per_id, permisos.atraqueFROM barcos, sobre, permisos, instalaciones,(SELECT * FROM para_a UNION SELECT ALL * FROM para_m) AS upam WHERE barcos.patente = sobre.patente and sobre.per_id = permisos.per_idand upam.iid = instalaciones.iid and permisos.per_id = upam.per_id and permisos.atraque = LIKE '%2020-08%') AS parte1 WHERE parte1.iid = pertenece.iid and pertenece.pid = puertos.pid) AS parte2GROUP BY parte2.nombre, parte2.atraqueORDER BY total DESC;
operator does not exist: timestamp without time zone ~~ unknown;
SELECT parte2.nombre, parte2.atraque, COUNT(parte2.nombre) as totalFROM (SELECT parte1.iid, parte1.patente, puertos.nombre, parte1.atraque FROM pertenece, puertos, (SELECT upam.iid, barcos.patente, barcos.pais, permisos.per_id, permisos.atraqueFROM barcos, sobre, permisos, instalaciones,(SELECT * FROM para_a UNION SELECT ALL * FROM para_m) AS upam WHERE barcos.patente = sobre.patente and sobre.per_id = permisos.per_idand upam.iid = instalaciones.iid and permisos.per_id = upam.per_id and permisos.atraque >= '2020-08-01' and permisos.atraque <= '2020-08-31') AS parte1 WHERE parte1.iid = pertenece.iid and pertenece.pid = puertos.pid) AS parte2GROUP BY parte2.nombre, parte2.atraqueORDER BY total DESC;
SELECT * FROM PUERTOS;
SELECT * FROM puertos, pertenece WHERE puertos.pid = pertenece.pid;
SELECT *FROM puertosOUTER JOIN perteneceON puertos.pid = pertenece.pid;
SELECT *FROM puertosLEFT JOIN pertenece ON puertos.pid = pertenece.pidUNIONSELECT *FROM puertosRIGHT JOIN pertenece ON puertos.pid = pertenece.pid;
SELECT parte2.nombre, parte2.atraque, COUNT(parte2.nombre) as totalFROM (SELECT parte1.iid, parte1.patente, puertos.nombre, parte1.atraque FROM pertenece, puertos, (SELECT upam.iid, barcos.patente, barcos.pais, permisos.per_id, permisos.atraqueFROM barcos, sobre, permisos, instalaciones,(SELECT * FROM para_a UNION SELECT ALL * FROM para_m) AS upam WHERE barcos.patente = sobre.patente and sobre.per_id = permisos.per_idand upam.iid = instalaciones.iid and permisos.per_id = upam.per_id and permisos.atraque >= '2020-08-01' and permisos.atraque <= '2020-08-31') AS parte1 WHERE parte1.iid = pertenece.iid and pertenece.pid = puertos.pid) AS parte2GROUP BY parte2.nombre, parte2.atraqueORDER BY total DESC;
SELECT MAX(parte3.count) FROM (SELECT parte2.nombre, COUNT (*) FROM (SELECT parte1.iid, parte1.patente, puertos.nombre, parte1.atraque FROM pertenece, puertos, (SELECT upam.iid, barcos.patente, barcos.pais, permisos.per_id, permisos.atraqueFROM barcos, sobre, permisos, instalaciones,(SELECT * FROM para_a UNION SELECT ALL * FROM para_m) AS upam WHERE barcos.patente = sobre.patente and sobre.per_id = permisos.per_idand upam.iid = instalaciones.iid and permisos.per_id = upam.per_id and permisos.atraque >= '2020-08-01' and permisos.atraque <= '2020-08-31') AS parte1 WHERE parte1.iid = pertenece.iid and pertenece.pid = puertos.pid) AS parte2 GROUP BY parte2.nombre) AS parte3;
SELECT MAX(parte3.count), parte3.nombre FROM (SELECT parte2.nombre, COUNT (*) FROM (SELECT parte1.iid, parte1.patente, puertos.nombre, parte1.atraque FROM pertenece, puertos, (SELECT upam.iid, barcos.patente, barcos.pais, permisos.per_id, permisos.atraqueFROM barcos, sobre, permisos, instalaciones,(SELECT * FROM para_a UNION SELECT ALL * FROM para_m) AS upam WHERE barcos.patente = sobre.patente and sobre.per_id = permisos.per_idand upam.iid = instalaciones.iid and permisos.per_id = upam.per_id and permisos.atraque >= '2020-08-01' and permisos.atraque <= '2020-08-31') AS parte1 WHERE parte1.iid = pertenece.iid and pertenece.pid = puertos.pid) AS parte2 GROUP BY parte2.nombre) AS parte3;
SELECT MAX(parte3.count), parte3.iid FROM (SELECT parte2.nombre, COUNT (*) FROM (SELECT parte1.iid, parte1.patente, puertos.nombre, parte1.atraque FROM pertenece, puertos, (SELECT upam.iid, barcos.patente, barcos.pais, permisos.per_id, permisos.atraqueFROM barcos, sobre, permisos, instalaciones,(SELECT * FROM para_a UNION SELECT ALL * FROM para_m) AS upam WHERE barcos.patente = sobre.patente and sobre.per_id = permisos.per_idand upam.iid = instalaciones.iid and permisos.per_id = upam.per_id and permisos.atraque >= '2020-08-01' and permisos.atraque <= '2020-08-31') AS parte1 WHERE parte1.iid = pertenece.iid and pertenece.pid = puertos.pid) AS parte2 GROUP BY parte2.nombre) AS parte3;
SELECT MAX(parte3.count), parte3 FROM (SELECT parte2.nombre, COUNT (*) FROM (SELECT parte1.iid, parte1.patente, puertos.nombre, parte1.atraque FROM pertenece, puertos, (SELECT upam.iid, barcos.patente, barcos.pais, permisos.per_id, permisos.atraqueFROM barcos, sobre, permisos, instalaciones,(SELECT * FROM para_a UNION SELECT ALL * FROM para_m) AS upam WHERE barcos.patente = sobre.patente and sobre.per_id = permisos.per_idand upam.iid = instalaciones.iid and permisos.per_id = upam.per_id and permisos.atraque >= '2020-08-01' and permisos.atraque <= '2020-08-31') AS parte1 WHERE parte1.iid = pertenece.iid and pertenece.pid = puertos.pid) AS parte2 GROUP BY parte2.nombre) AS parte3;
SELECT MAX(parte3.count) FROM (SELECT parte2.nombre, COUNT (*) FROM (SELECT parte1.iid, parte1.patente, puertos.nombre, parte1.atraque FROM pertenece, puertos, (SELECT upam.iid, barcos.patente, barcos.pais, permisos.per_id, permisos.atraqueFROM barcos, sobre, permisos, instalaciones,(SELECT * FROM para_a UNION SELECT ALL * FROM para_m) AS upam WHERE barcos.patente = sobre.patente and sobre.per_id = permisos.per_idand upam.iid = instalaciones.iid and permisos.per_id = upam.per_id and permisos.atraque >= '2020-08-01' and permisos.atraque <= '2020-08-31') AS parte1 WHERE parte1.iid = pertenece.iid and pertenece.pid = puertos.pid) AS parte2 GROUP BY parte2.nombre) AS parte3;
\c grupo75e2
SELECT parte1.iid, parte1.patente, parte1.nombre, ubicacion.ciudad, puertos.nombre, parte1.atraque FROM pertenece, puertos, esta, ubicacion, (SELECT upam.iid, barcos.patente, barcos.nombre, barcos.pais, permisos.per_id, permisos.atraqueFROM barcos, sobre, permisos, instalaciones,(SELECT * FROM para_a UNION SELECT ALL * FROM para_m) AS upam WHERE barcos.patente = sobre.patenteand barcos.nombre = 'Calypso' and sobre.per_id = permisos.per_idand upam.iid = instalaciones.iid and permisos.per_id = upam.per_id) AS parte1 WHERE parte1.iid = pertenece.iid and pertenece.pid = puertos.pid and puertos.pid = esta.pidand esta.ciudad = ubicacion.ciudad and ubicacion.ciudad = 'Arica';
\q
\c grupo75e2
DROP TABLE pertenece;
Create table pertenece(iid INT, pid INT, PRIMARY KEY(iid));
select * from pertenece;
exit
\q
\c grupo75e2
select * from pertenece;
cd Entrega2
\c Entrega2
\q
\c grupo75e2
\ COPY Pertenece from 'pertenece.csv' DELIMITER ',' CSV HEADER;
\COPY Pertenece from 'pertenece.csv' DELIMITER ',' CSV HEADER;
DROP TABLE Pertenece;
CREATE TABLE Pertenece(iid INT, pid INT, PRIMARY KEY(iid,pid));
select * from pertenece;
\COPY Pertenece from 'pertenece.csv' DELIMITER ',' CSV HEADER;
select * from pertenece;
drop table pertenece;
CREATE TABLE Pertenece(iid INT, pid INT, PRIMARY KEY(iid,pid));
\q
\c grupo75e2
 \COPY Pertenece from 'pertenece.csv' DELIMITER ',' CSV HEADER;
DROP TABLE pertenece;
create table pertenece(iid INT, pid INT, PRIMARY KEY(iid,pid));
 \COPY Pertenece from 'pertenece.csv' DELIMITER ',' CSV HEADER;
\q
\c grupo75e2
select * from pertenece;
 \COPY Pertenece from 'pertenece.csv' DELIMITER ',' CSV HEADER;
select * from pertenece, puertos;
select distinct * from puertos, pertenece;
select * from puertos;
select * from pertenece;
\c grupo75e3
cd C:\Users\lulid\Documents\Universidad\BDD\proyecto_BDD
cd "C:\Users\lulid\Documents\Universidad\BDD\proyecto_BDD"
\cd C:\Users\lulid\Documents\Universidad\BDD\proyecto_BDD
\cd Users\lulid\Documents\Universidad\BDD\proyecto_BDD
exit
\q
\c grupo75e3
\COPY fechas from "fechas.csv" DELIMITER "," CSV HEADER;
\dt
COPY fechas from "fechas.csv" DELIMITER "," CSV HEADER;
\cd Entrega2
\COPY fechas from "fechas.csv" DELIMITER "," CSV HEADER;
\COPY fechas from 'fechas.csv' DELIMITER "," CSV HEADER;
exit
\q
\c grupo75e3
\COPY fechas from 'C:\Users\lulid\Documents\Universidad\BDD\proyecto_BDD\fechas.csv' DELIMITER "," CSV HEADER;
\COPY fechas from 'Users\lulid\Documents\Universidad\BDD\proyecto_BDD\fechas.csv' DELIMITER "," CSV HEADER;
\COPY fechas from 'Users\lulid\Documents\Universidad\BDD\proyecto_BDD\fechas.csv' DELIMITER  ',' CSV HEADER;
\q
\c grupo75e3
\COPY fechas from 'proyecto_BDD\fechas.csv' DELIMITER  ',' CSV HEADER;
SELECT * FROM fechas;
cd proyecto_BDD
\q
\c grupo75e2
\dt
\q
\c grupo75e2
\i ocupacion_fechas.sql
\i ocupacion_fechas.sql
\q
\c grupo75e2
\i ocupacion_fechas.sql
git pull origin masterexit+
\q
\c grupo75e2
\i ocupacion_fechas.sql
\q
\c grupo75e2
\i ocupacion_fechas.sql
SELECT ocupacion_fechas('2018-02-02','2018-04-04');
\q
\c grupo75e2
\i ocupacion_fechas.sql
SELECT ocupacion_fechas('2018-02-02','2018-04-04');
\q
\c grupo75e2
\i ocupacion_fechas.sql
SELECT ocupacion_fechas('2018-02-02','2018-04-04');
\c grupo75e2
\i ocupacion_fechas.sql
\q
\c grupo75e2
\i ocupacion_fechas.sql
\q
\c grupo75e2
\i ocupacion_fechas.sql
\1
\q
\c grupo75e2
\i ocupacion_fechas.sql
SELECT ocupacion_fechas('2009-02-02','2020-02-02');
\q
\c grupo75
\i ocupacion_fechas.sql
SELECT ocupacion_fechas('2009-02-02','2020-02-02');
\dt
\q
\c grupo75e2
SELECT A.iid, A.capacidad, permisos.per_id,permisos.atraque,permisos_astilleros.atraqueFROM para_a,permisos_astilleros,(SELECT instalaciones.iid, instalaciones.capacidadFROM instalaciones, astilleros WHERE instalaciones.iid = astilleros.iid) as AWHERE para_a.iid = A.iid and permisos_astilleros.per_id = para_a.per_id and permisos.per_id = permisos_astilleros.per_id;SELECT A.iid, A.capacidad, permisos.per_id,permisos.atraque,permisos_astilleros.atraque
FROM para_a,permisos_astilleros,(SELECT instalaciones.iid, instalaciones.capacidadFROM instalaciones, astilleros WHERE instalaciones.iid = astilleros.iid) as AWHERE para_a.iid = A.iid and permisos_astilleros.per_id = para_a.per_id and permisos.per_id = permisos_astilleros.per_id;
SELECT A.iid, A.capacidad, permisos.per_id,permisos.atraque,permisos_astilleros.atraqueFROM para_a,permisos_astilleros,permisos,(SELECT instalaciones.iid, instalaciones.capacidadFROM instalaciones, astilleros WHERE instalaciones.iid = astilleros.iid) as AWHERE para_a.iid = A.iid and permisos_astilleros.per_id = para_a.per_id and permisos.per_id = permisos_astilleros.per_id;
SELECT A.iid, A.capacidad, permisos.per_id,permisos.atraque,permisos_astilleros.salidaFROM para_a,permisos_astilleros,permisos,(SELECT instalaciones.iid, instalaciones.capacidadFROM instalaciones, astilleros WHERE instalaciones.iid = astilleros.iid) as AWHERE para_a.iid = A.iid and permisos_astilleros.per_id = para_a.per_id and permisos.per_id = permisos_astilleros.per_id;
\dt
select * from jefe;
SELECT A.iid, A.capacidad, permisos.per_id,permisos.atraque,permisos_astilleros.salidaFROM para_a,permisos_astilleros,permisos,(SELECT instalaciones.iid, instalaciones.capacidadFROM instalaciones, astilleros WHERE instalaciones.iid = astilleros.iid) as AWHERE para_a.iid = A.iid and permisos_astilleros.per_id = para_a.per_id and permisos.per_id = permisos_astilleros.per_id;
SELECT A.iid, A.capacidad, permisos.per_id,permisos.atraque,permisos_astilleros.salidaFROM para_a,permisos_astilleros,permisos,(SELECT instalaciones.iid, instalaciones.capacidadFROM instalaciones, astilleros WHERE instalaciones.iid = astilleros.iid) as AWHERE para_a.iid = A.iid and permisos_astilleros.per_id = para_a.per_id and permisos.per_id = permisos_astilleros.per_id;
\c grupo75e2
\i ocupacion_fechas.sql
SELECT ocupacion_fechas('2009-02-02','2020-02-02');
\q
\c grupo75e2
\i ocupacion_fechas.sql
SELECT ocupacion_fechas('2009-02-02','2020-02-02');
\q
\c grupo75e2
\i ocupacion_fechas.sql
\q
\c grupo75e2
\i ocupacion_fechas.sql
\q
\c grupo75e2
\i ocupacion_fechas.sql
\q
\c grupo75e2
\i ocupacion_fechas.sql
\q
\c grupo75e2
\i ocupacion_fechas.sql
\q
\c grupo75e2
\i ocupacion_fechas.sql
\q
\c grupo75e2
\i ocupacion_fechas.sql
SELECT ocupacion_fechas('2009-02-02','2020-02-02');
\q
\c grupo75e2
\i ocupacion_fechas.sql
SELECT ocupacion_fechas('2009-02-02','2020-02-02');
git pull
\q
\c grupo75e2
\i ocupacion_fechas.sql
SELECT ocupacion_fechas('2009-02-02','2020-02-02');
SELECT A.iid, A.capacidad, permisos.per_id,permisos.atraque,permisos_astilleros.salidaFROM para_a,permisos_astilleros,permisos,(SELECT instalaciones.iid, instalaciones.capacidadFROM instalaciones, astilleros WHERE instalaciones.iid = astilleros.iid) as AWHERE para_a.iid = A.iid and permisos_astilleros.per_id = para_a.per_id and permisos.per_id = permisos_astilleros.per_id;
\q
\c grupo75e2
\i ocupacion_fechas.sql
SELECT ocupacion_fechas('2009-02-02','2020-02-02');
\c grupo75e2
\i capacidad.sql
SELECT capacidad('2009-02-02','2020-02-02');
\q
\c grupo75e2
\i capacidad.sql
SELECT capacidad('2009-02-02','2020-02-02');
\q
\c grupo75e2
\i capacidad.sql
SELECT capacidad('2009-02-02','2020-02-02');
\q
\c grupo75e2
\i capacidad.sql
 DROP FUNCTION capacidad(date,date);
\i capacidad.sql
SELECT capacidad('2009-02-02','2020-02-02');
\q
\c grupo75e2
\i capacidad.sql
SELECT capacidad('2009-02-02','2020-02-02');
\q
\c grupo75e2
\i capacidad.sql
SELECT capacidad('2009-02-02','2020-02-02');
\q
\c grupo75e2
\i capacidad.sql
SELECT capacidad('2009-02-02','2020-02-02');
\q
\c grupo75e2
\i capacidad.sql
DROP FUNCTION capacidad(date,date);
\i capacidad.sql
SELECT capacidad('2009-02-02','2020-02-02');
\q
\i promedio.sql
\q
\c grupo75e2
\i promedio.sql
\q
\c grupo75e2
\i promedio.sql
SELECT promedio('2009-02-02','2020-02-02');
\q
\c grupo75e2
\i promedio.sql
SELECT promedio('2009-02-02','2020-02-02');
\1
\q
\c grupo75e2
\i promedio.sql
\1
\q
\c grupo75e2
\i promedio.sql
SELECT promedio('2009-02-02','2020-02-02');
\q
\c grupo75e2
\i promedio.sql
DROP FUNCTION promedio(date,date);
\i promedio.sql
SELECT promedio('2009-02-02','2020-02-02');
\q
\c grupo75e2
\i capacidad.sql
SELECT capacidad('2009-02-02','2020-02-02','Quintero');
\q
\c grupo75e2
\i capacidad.sql
SELECT capacidad('2009-02-02','2020-02-02','Quintero');
\q
\c grupo75e2
\i capacidad.sql
\i promedio.sql
SELECT promedio('2009-02-02','2020-02-02','Quintero');
\q
\c grupo75e2
\i promedio.sql
SELECT promedio('2009-02-02','2020-02-02','Quintero');
\q
\c grupo75e2
\i promedio.sql
SELECT promedio('2009-02-02','2020-02-02','Quintero');
\q
\c grupo75e2
\i promedio.sql
SELECT promedio('2009-02-02','2020-02-02','Quintero');
\q
\c grupo75e2
SELECT capacidad('2009-02-02','2020-02-02','Quintero');
SELECT promedio('2009-02-02','2020-02-02','Quintero');
\q
\c grupo75e2
\i as_capacidad.sql
\q
\c grupo75e2
\i as_capacidad.sql
\q
\c grupo75e2
\i as_capacidad.sql
SELECT as_capacidad('2009-02-02','2020-02-02','Quintero','DK4889');
\q
\c grupo75e2
\i as_capacidad.sql
SELECT as_capacidad('2009-02-02','2020-02-02','Quintero','DK4889');
drop function as_capacidad;
\i as_capacidad.sql
SELECT as_capacidad('2009-02-02','2020-02-02','Quintero','DK4889');
\q
\c grupo75e2
\i as_capacidad.sql
SELECT as_capacidad('2009-02-02','2020-02-02','Quintero','DK4889');
select * froma  para_a;
select * from para_a;
SELECT as_capacidad('2009-02-02','2020-02-02','Quintero','DK4889');
q
\q
\c grupo75e2
\i as_capacidad.sql
\q
\c grupo75e2
\i as_capacidad.sql
SELECT as_capacidad('2009-02-02','2020-02-02','Quintero','DK4889');
\q
\c grupo75e2
\i as_capacidad.sql
SELECT as_capacidad('2009-02-02','2020-02-02','Quintero','DK4889');
\q
\c grupo75e2
\i as_capacidad.sql
\q
\c grupo75e2
\i as_capacidad.sql
\q
\c grupo75e2
DROP FUCTION as_capacidad;
drop function as_capacidad;
\i as_capacidad.sql
\q
\c grupo75e2
\i as_capacidad.sql
SELECT as_capacidad('2009-02-02','2020-02-02','Quintero','DK4889');
\q
\c grupo75e2
\i as_capacidad.sql
SELECT as_capacidad('2009-02-02','2020-02-02','Quintero','DK4889');
\Q
\q
\c grupo75e2
\i as_capacidad.sql
SELECT as_capacidad('2009-02-02','2020-02-02','Quintero','DK4889');
\c grupo75e2
Select * from instalaciones Union muelles;
select * from (astilleros Union muelles) as juntos;
Select * from astillerosUnion Select * from muelles ;
\dt
select * from astilleros;
\q
\c grupo75e3
Select * from astillerosUnion Select * from muelles ;
Select * from jefe, (Select puertos.pid, puertos.nombre, instalaciones.iidFrom puertos, pertenece, instalacionesWhere puertos.pid = pertenece.pid and pertenece.iid = instalaciones.iid) as instwhere jefe.iid = inst.iid ;
Select * from jefe, (Select * from astillerosUnion Select * from muelles) as juntos,(Select puertos.pid, puertos.nombre, instalaciones.iidFrom puertos, pertenece, instalacionesWhere puertos.pid = pertenece.pid and pertenece.iid = instalaciones.iid) as instwhere jefe.iid = inst.iid juntos.iid = inst.iid ;
Select * from jefe, (Select * from astillerosUnion Select * from muelles) as juntos,(Select puertos.pid, puertos.nombre, instalaciones.iidFrom puertos, pertenece, instalacionesWhere puertos.pid = pertenece.pid and pertenece.iid = instalaciones.iid) as instwhere jefe.iid = inst.iid and juntos.iid = inst.iid ;
Select jefe.rut, inst.iid, inst.nombre,juntos.tipo from jefe, (Select * from astillerosUnion Select * from muelles) as juntos,(Select puertos.pid, puertos.nombre, instalaciones.iidFrom puertos, pertenece, instalacionesWhere puertos.pid = pertenece.pid and pertenece.iid = instalaciones.iid) as instwhere jefe.iid = inst.iid and juntos.iid = inst.iid ;
\c grupo75e3
\dt
SELECT jefe.rut, inst.iid, inst.nombre, juntos.tipo FROM jefe, (SELECT * FROM astilleros UNION SELECT * FROM muelles) as juntos, (SELECT puertos.pid, puertos.nombre, instalaciones.iid FROM puertos, pertenece, instalaciones WHERE puertos.pid = pertenece.pid AND pertenece.iid = instalaciones.iid) as inst WHERE jefe.iid = inst.iid AND juntos.iid = inst.iid;
SELECT * FROM jefe;
SELECT jefe.rut, inst.iid, inst.nombre, juntos.tipo FROM jefe, (SELECT * FROM astilleros UNION SELECT * FROM muelles) as juntos, (SELECT puertos.pid, puertos.nombre, instalaciones.iid FROM puertos, pertenece, instalaciones WHERE puertos.pid = pertenece.pid AND pertenece.iid = instalaciones.iid) as inst WHERE jefe.iid = inst.iid AND juntos.iid = inst.iid;
SELECT jefe.rut, inst.iid, inst.nombre, juntos.tipo FROM jefe, (SELECT * FROM astilleros UNION SELECT * FROM muelles) as juntos, (SELECT puertos.pid, puertos.nombre, instalaciones.iid FROM puertos, pertenece, instalaciones WHERE puertos.pid = pertenece.pid AND pertenece.iid = instalaciones.iid) as inst WHERE jefe.iid = inst.iid AND juntos.iid = inst.iid;
SELECT jefe.rut, inst.iid, inst.nombre, juntos.tipo FROM jefe, (SELECT * FROM astilleros UNION SELECT * FROM muelles) as juntos, (SELECT puertos.pid, puertos.nombre, instalaciones.iid FROM puertos, pertenece, instalaciones WHERE puertos.pid = pertenece.pid AND pertenece.iid = instalaciones.iid) as inst WHERE jefe.iid = inst.iid AND juntos.iid = inst.iid AND jefe.rut = '75020925-4';
SELECT jefe.rut, inst.nombre, juntos.tipo FROM jefe, (SELECT * FROM astilleros UNION SELECT * FROM muelles) as juntos, (SELECT puertos.pid, puertos.nombre, instalaciones.iid FROM puertos, pertenece, instalaciones WHERE puertos.pid = pertenece.pid AND pertenece.iid = instalaciones.iid) as inst WHERE jefe.iid = inst.iid AND juntos.iid = inst.iid AND jefe.rut = '75020925-4';
\q
\c grupo75e3
\q
\c grupo75e3
\i as_capacidad.sql;
\i mu_capacidad.sql;
\i promedio.sql;
\i capacidad.sql;
jtaranguiz@uc.cl;
Select ('2015-02-03', '2016-02-04')Select capacidad(('2015-02-03', '2016-02-04'));
Select capacidad('2015-02-03', '2016-02-09','Guayacan');
\c grupo75e3
\dt
SELECT * FROM puertos;
SELECT * FROM fechas;
SELECT * FROM cierre;
\q
\c grupo75e3
\dt
SELECT * FROM barcos;
\q
\c grupo75e3
SELECT mu_capacidad('2018-01-01','Penco','DK4889');
SELECT as_capacidad('2018-01-01','2018-12-31','Penco','DK4889');
SELECT as_capacidad('2012-01-01','2018-12-31','Penco','DK4889');
\q
\c grupo75e3
SELECT mu_capacidad('2018-04-04",'Guayacan','Readme:Para esta entrega hicimos los siguientes supuestos:- para las consultas almacenadas los días en los que no llegó ningún barco, no aparecen pero tienen la capacidad completa. Por ejemplo, si el promedio calculado de ocupación es 0 en ese periodo, no aparece en la lista.- la contraseña inicial para todos es '123456789' y después cada usuario puede actualizarlo.- la importación de usuarios (desde capitanes y jefes) se hizo por python según lo respondido en el issue 312.- si en el botón para generar un permiso en astillero o muelle, no se imprime nada, es mejor agrandar el intervalo de tiempo para visualizar mejor (no imprime los que tienen ocupación 0).);
SELECT mu_capacidad('2018-04-04",'Guayacan','DK4889');SELECT mu_capacidad('2018-04-04",'Guayacan','Readme:Para esta entrega hicimos los siguientes supuestos:- para las consultas almacenadas los días en los que no llegó ningún barco, no aparecen pero tienen la capacidad completa. Por ejemplo, si el promedio calculado de ocupación es 0 en ese periodo, no aparece en la lista.- la contraseña inicial para todos es '123456789' y después cada usuario puede actualizarlo.- la importación de usuarios (desde capitanes y jefes) se hizo por python según lo respondido en el issue 312.- si en el botón para generar un permiso en astillero o muelle, no se imprime nada, es mejor agrandar el intervalo de tiempo para visualizar mejor (no imprime los que tienen ocupación 0).);;";);
SELECT mu_capacidad('2018-12-31','Penco','DK4889');
SELECT mu_capacidad('2011-08-08','Iquique','DK4889');
SELECT mu_capacidad('2011-08-08','Punta Patache','DK4889');
SELECT mu_capacidad('2011-08-08','Quintero','DK4889');
psql
\q
\c grupo75e2
SELECT mu_capacidad('2011-08-08','Quintero','DK4889');
\q
\c grupo75e2
\i mu_capacidad;
\i mu_capacidad.sql;
SELECT mu_capacidad('2011-08-08','Quintero','DK4889');
\1
\q
\c grupoe2
\c grupo75e2
\i mu_capacidad.sql;
\q
\c grupo75e2
\i mu_capacidad.sql;
\q
\c grupo75e2
\i mu_capacidad.sql;
\q
\c grupo75e2
\i mu_capacidad.sql;
SELECT mu_capacidad('2011-08-08','Quintero','DK4889');
\q
\c grupo75e2
\i mu_capacidad.sql;
SELECT mu_capacidad('2011-08-08','Quintero','DK4889');
\q
\c grupo75e3
\i mu_capacidad.sql;
SELECT mu_capacidad('2011-08-08','Quintero','DK4889');
select * from ResultadosPleb;
select * from municipios;
\q
\c grupo75e2
\q
